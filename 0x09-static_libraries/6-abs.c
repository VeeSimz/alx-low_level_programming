#include "main.h"
/**
 * _abs - computes absolute value of an integer
 *
 * @i: integer input to check
 *
 * Return: value of int
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
